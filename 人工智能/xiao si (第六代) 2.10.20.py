print('-------------------------本产品是邵宗贤独家制作,人工智能-小思2022年的第6代的产物-----------------------')
print('--小思网络叠加系统-11月份出版')
import json as j
import pyttsx3
import requests
import socket
import uuid
from urllib.request import urlopen, Request
from urllib.parse import urlencode
print('你好,我是小思')
print('第6代人工智能')
engline = pyttsx3.init()
engline.setProperty('voice','zh')
line1 = '你好，主人'
line2 = '欢迎光临'
line3 = '我是小思同学'
line4 = '小思的第6代产品(six generation)'
line5 = '欢迎为您服务'
line6 = 'Hi,my name is Small thinking'
line7 = "Welcome to Small thinking world"
line8 = "Welcome to serve you"
engline.say(line1)
engline.say(line2)
engline.say(line3)
engline.say(line4)
engline.say(line5)
engline.say(line6)
engline.say(line7)
engline.say(line8)
engline.runAndWait()
class Turing_robot(object):
    while True:
            a = input()
            url = 'https://api.ownthink.com/bot?appid=9ffcb5785ad9617bf4e64178ac64f7b1&spoken=%s' %a
            te = requests.get(url).json()
            data = te['data']['info']['text']
            print(data)
            ini = pyttsx3.init()
            shuo = ini.say(data)
            shuo = ini .say('daya')
            ini.runAndWait()
engine = pyttsx3.init()
def getTuringText(self,text):
    user_ip = self.getHostIP()
    mac_id = self.getMacID()
    turing_url_data = dict(key=self.app_key,info=text,userid=mac_id)
def getHostIP(self):
    socket_info = socket.socket(socket.AF_INET,socket.SOCK_DGRAM)
    socket_info.connect('8.8.8.8', 80)
    ip = socket_info.getsockname()[0]
    return ip
def getMaID(self):
    node = uuid.getnode()

    mac = uuid.UUID(int=node).hex[-12:]
    return mac
voices=1
voices=0
""" RATE"""
rate = engine.getProperty('rate')
print (rate)
engine.setProperty('rate', 125)
int (voices)
engine.setProperty('volume',1.0)

"""VOICE"""
voices = engine.getProperty('voices')
engine.setProperty('voice', voices[0].id)
engine.setProperty('voice', voices[1].id)

engine.say("Hello,Windows")
engine.say('my name is xiao si')
engine.runAndWait()
engine.stop()


""" RATE"""
rate = engine.getProperty('rate')
print (rate)
engine.setProperty('rate', 125)


"""VOLUME"""
volume = engine.getProperty('volume')
print (volume)
engine.setProperty('volume',1.0)

"""VOICE"""
voices = engine.getProperty('voices')

engine.setProperty('voice', voices[1].id)

def getTuringText(self,text):
    user_ip = self.getHostIP()
    mac_id = self.getMacID()
    turing_url_data = dict(key=self.app_key,info=text,userid=mac_id)
def getHostIP(self):
    socket_info = socket.socket(socket.AF_INET,socket.SOCK_DGRAM)
    socket_info.connect('8.8.8.8', 80)
    ip = socket_info.getsockname()[0]
    return ip
def getMaID(self):
    node = uuid.getnode()
    mac = uuid.UUID(int=node).hex[-12:]
    return mac
def getMaID(self):
    node = uuid.getnode('voices')

voices=1
voices=0
""" RATE"""
rate = engine.getProperty('rate')
print (rate)
engine.setProperty('rate', 125)
int (voices)
engine.setProperty('volume',1.0)

"""VOICE"""
voices = engine.getProperty('voices')
engine.setProperty('voice', voices[0].id)
engine.setProperty('voice', voices[1].id)

engine.say("Hello,Windows")
engine.say('my name is xiao si')
engine.runAndWait()
engine.stop()


""" RATE"""
rate = engine.getProperty('rate')
print (rate)
engine.setProperty('rate', 125)


"""VOLUME"""
volume = engine.getProperty('volume')
print (volume)
engine.setProperty('volume',1.0)

"""VOICE"""
voices = engine.getProperty('voices')

engine.setProperty('voice', voices[1].id)

def getMaID(self):
    node = uuid.getnode()
    mac = uuid.UUID(int=node).hex[-12:]
    return mac
def getMaID(self):
    node = uuid.getnode('voices')
voices = 3
voices = 4
class Turing_robot(object):
    while True:
            a = input()
            url = 'https://api.ownthink.com/bot?appid=9ffcb5785ad9617bf4e64178ac64f7b1&spoken=%s' %a
            te = requests.get(url).json()
            data = te['data']['info']['text']['daya']
            print(data)
            print('daya')
            def getMaID(self):
                uuid.getnode('voices')
                def getMaID(self):
                    j.data = j.load('https://api.ownthink.com/bot?appid=9ffcb5785ad9617bf4e64178ac64f7b1&spoken=%s')
                    node = uuid.getnode('voices')
            ini = pyttsx3.init()
            shuo = ini.say(data)
            voices = 5
            voices = 6
            shao = 7
            shao = 8
            ini.runAndWait('voices')
            ini.runAndWait('shao')
            engine.setProperty('voice', voices[0].id)
            engine.setProperty('voice', voices[1].id)
            j.data = j.load(open('my_file.json', 'r'))
            print(j.load(open('my_file.json', 'j')))
            def grtMaID(self):
                node = voices = j = rate = id()
                url = voices = node = rate = j
                node = shao = j = reta = id()
                url = shao = node = rate = j
            def geipandas(help):
                j.decoder(rate)
                redHad = ('self')
                redHad = (print(j))
                def gripMaID(help):
                    ini = pyttsx3.init(voices)
                    ini = pyttsx3.init('shao')
                def getMaID(self):
                    uuid.getnode('voices')
                    def getMaID(self):
                        j.data = j.load('https://api.ownthink.com/bot?appid=9ffcb5785ad9617bf4e64178ac64f7b1&spoken=%s')
                        node = uuid.getnode('voices')





